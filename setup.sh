#!/bin/bash

# Install required Python packages
echo "Installing dependencies..."
pip install -r requirements.txt

echo "Setup complete. Run the application with 'python run.py' or 'streamlit run app/main.py'" 